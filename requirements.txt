# Setuptools first (aktuell halten für Build-Probleme in HF Spaces)
setuptools>=75.0.0
wheel>=0.44.0

# NumPy (WICHTIG: <2.0 halten für viele Libs-Kompatibilität!)
numpy>=1.24.0,<2.0.0

# FastAPI & Server (upgedatet auf aktuellste stabile)
fastapi==0.133.0
uvicorn[standard]==0.32.0          # Neueste stabile, bessere Performance
python-multipart==0.0.12           # Neueste, Bugfixes
websockets==13.0                   # Aktuell

# Database
sqlalchemy==2.0.35                 # Neueste Minor, stabil

# Authentication (argon2 empfohlen – bcrypt-Probleme weg)
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0

# Pydantic & Settings (upgedatet, kompatibel mit FastAPI 0.133)
pydantic==2.9.2
pydantic-settings==2.5.2           # Neueste
email-validator==2.2.0             # Neueste

# LLM & AI – LOKAL / QUANTIZED (kein externer HF Inference mehr!)
transformers==5.2.0                # Aktuellste stabile (Feb 2026)
torch==2.4.1                       # Kompatibel mit transformers 5.x, HF Spaces baut passend
accelerate==1.0.0                  # Stabil für device_map="auto" (neuere 1.12 möglich, aber teste erst)
bitsandbytes==0.49.2               # Neueste für 4-bit/8-bit Quant (Feb 2026 Release)
sentence-transformers==3.1.1       # Upgedatet, bessere Embeddings für RAG

# TTS (aktuell halten)
gTTS==2.5.1                        # Kleine Bugfix-Release

# RAG
faiss-cpu==1.8.0                   # Neueste CPU-Version, besser Performance

# Utilities
python-dotenv==1.0.1
pytz==2024.2                       # Aktuell
requests==2.32.3                   # Sicherheitsupdates
pillow==10.4.0                     # Neueste
aiofiles==24.1.0                   # Neueste

# Audio processing
librosa==0.10.2                    # Kleine Updates
soundfile==0.13.0                  # Neueste

# Optional: Falls du später vLLM testen willst (schneller auf GPU/ZeroGPU)
# vllm==0.6.5                      # Nur auskommentieren, wenn ZeroGPU large aktiv & getestet
